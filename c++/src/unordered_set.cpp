// #include "unordered_set.hpp"


// template<typename T>
// UnorderedSet<T>::UnorderedSet() : UnsorteSetI<T>(), _buckets(nullptr),
//                                   _capacity(0) {
//     //
// }

// template<typename T>
// UnorderedSet<T>::UnorderedSet(size_t cap) : UnsorteSetI<T>(),
// _buckets(nullptr),
//                                             _capacity(cap) {
//     //
// }

// template<typename T>
// UnorderedSet<T>::~UnorderedSet() {
//     //
// }


// template<typename T>
// bool UnorderedSet<T>::insert (const T& elem) {
//     //
// }

// template<typename T>
// bool UnorderedSet<T>::remove (const T& i) {
//     //
// }

// template<typename T>
// bool UnorderedSet<T>::find (const T& elem) {
//     //
// }


// template<typename T>
// uint32_t UnorderedSet<T>::hash (const T& elem) const {
//     //
// }
